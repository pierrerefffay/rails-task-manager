<h1>My task</h1>
<% @tasks.each do |task| %>
    <h4><input type="checkbox" name=""><%=  link_to task.title, show_task_path(task) %></h4>
      <%= link_to "Destroy", delete_task_path(task),
        method: :delete,
        data: { confirm: "Are you sure?" } %>
  <% end %>



  <h6><%=  link_to "add a task", new_path %> </h6>

